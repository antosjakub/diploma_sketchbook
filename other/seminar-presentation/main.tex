\documentclass{beamer}

% Packages (if needed)
\usepackage{graphicx}
\usepackage{listings}

% Presentation information
\title{Your Presentation Title}
\subtitle{Optional Subtitle}
\author{Your Name}
\institute{Your University/Institution}
\date{\today}

\begin{document}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% Title slide
\frame{\titlepage}

%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{Problem statement}

\pause
Let
\begin{itemize}
\item $\Omega \subset \mathbb{R}^d$ be open and bounded
\item $T \in (0, \infty)$
\item $Q_T = (0,T) \times \Omega$
\end{itemize}

\pause
Consider
\begin{align*}
\partial_t u + L\,u = f \quad &\text{in } Q_T \\
u(0,x) = u_{IC}(x)\quad &\text{on } \Omega \\
u(t,x) = u_{BC}(t,x)\quad &\text{on } (0,T)\times \Omega
\end{align*}

\pause
Where
\begin{itemize}
\item $L\,u = -\alpha\,\Delta u + \vec{b} \cdot \nabla u + c\, u$
\pause
\item $u,f,c,u_{BC}: Q_T \to \mathbb{R}$
\item $u_{IC}: \Omega \to \mathbb{R}$
\item $b: Q_T \to \mathbb{R}^d$
\end{itemize}

\end{frame}
%%%%%%%%%%%%%%%% End

%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{Grid based solvers: FDM (Metoda konecnych differenci)}

Now take
$$ \vec{b}=\vec{0}, \quad c = 0, \quad u_{BC} = 0, \Omega = (0,1)^d $$
and consider the system
\begin{align*}
\partial_t u - \alpha\,\Delta u = f \quad &\text{in } Q_T \\
u(0,x) = u_{IC}(x)\quad &\text{on } \Omega \\
u(t,x) = 0\quad &\text{on } (0,T)\times \Omega
\end{align*}

\end{frame}
%%%%%%%%%%%%%%%% End

%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{FDM: discretize}

$$ \partial_t u - \alpha\,\Delta u = f $$

Finite differences

- Forward difference in time

$\partial u / \partial t \approx \frac{u(t+\tau,x) - u(t,x)}{\tau} $

- Central difference in space

$\partial u / \partial x \approx \frac{u(t,x+h/2) - u(t,x-h/2)}{h} $

$\partial^2 u /\partial x^2 \approx \frac{u(t,x+h) - 2u(t,x) + u(t,x-h)}{h^2} $

Create a uniform grid in time and space

$$ \{x_i\}_{i=0}^{n+1} = (x_0, x_1, ..., x_n, x_{n+1}), \quad x_0 = 0.0, \quad x_{n+1} = 1.0 $$

Define

$$ u^t_i = u(t^n, x_i) $$



\end{frame}
%%%%%%%%%%%%%%%% End

%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{FDM: Towards the $A\,x=b$}

implicit > explicit (explicit needs $\alpha \tau / h^2 \le 1/2$)

$$
\frac{u_i^{t+1} - u_i^{t}}{\tau}
- \alpha\, \frac{u_{i+1}^{t+1} - 2u_i^{t+1} + u_{i-1}^{t+1}}{h^2} = f_i^{t+1}
$$

$$
u_i^{t+1} -
\frac{\alpha\,\tau}{h^2} (u_{i+1}^{t+1} - 2u_i^{t+1} + u_{i-1}^{t+1})
= u_i^{t} + \tau f_i^{t+1}
$$

$$
U^{t+1} -
\frac{\alpha\,\tau}{h^2} L\,U^{t+1}
= (I - \frac{\alpha\,\tau}{h^2} L)\, U^{t+1}
= U^t + \tau F^{t+1}
$$

To get $U^{t+1}$ from $U^{t}$, we need to solve:

$$
(I - \frac{\alpha\,\tau}{h^2} L)\, U^{t+1}
= U^t + \tau F^{t+1}
$$

We need to solve $A\,x=b$, where $x$ and $b$ are of size $N=n^d$.
\begin{itemize}
\item $A = (I - \frac{\alpha\,\tau}{h^2} L)$
\item $x = U^{t+1}$
\item $b = U^t + \tau F^{t+1}$
\end{itemize}

\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{FDM: The Curse of Dimensionality}

Solve $A\,x=b$, where $x$ and $b$ are of size $N=n^d$.
\begin{itemize}
\item $A = (I - \frac{\alpha\,\tau}{h^2} L)$
\item $x = U^{t+1}$
\item $b = U^t + \tau F^{t+1}$
\end{itemize}

Suppose $n=64$:
%\begin{center}
\begin{tabular}{ c c c c c c c c c }
 $d$ & 1 & 2 & 3 & 4 & 5 & 6 & 7 & 8\\ 
 $n^d$ & 64 & 4,096 & 262,144 & 16.8e6 & 1.07e9 & 6.87e10 & 4.40e12 & 2.81e14
\end{tabular}
%\end{center}

\pause
$\implies$  The Curse of Dimensionality (prokleti?) \\

\pause
\textbf{Question: Stiffness matrix A is sparse (ridka), symmetric, positive definite - will it be enough to make any computation feasible?}

\pause
\textbf{Answer: Yes!}\pause\textbf{ But not really...}

\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}[fragile]
\frametitle{FDM: The Curse of Dimensionality}

\textbf{Answer: Yes!}
\pause
\begin{itemize}
    \item Conjugate gradients (CG)
    \begin{itemize}
        \item solves Ax=b, for x where A is a symmetric, positive-definite linear operator
        \item uses short recurrences $\implies$ fixed memory costs and fixed computational costs per iteration
    \end{itemize}
\end{itemize}

\pause
\textbf{Answer: Not really...}
\pause
\begin{itemize}
    \item Problem = memory
\end{itemize}

\pause
\begin{lstlisting}[breaklines=true,frame=single,basicstyle=\footnotesize\ttfamily]
X = get_grid_as_1d_vector(n,d); # N x d
U = u_IC.(X, t=0.0)             # N
L = get_laplace_matrix()        # N x (2d+1)
I = sparse()                    # N
A = I - r * L                   # N x (2d+1)
                        # total = N x (5d+4)
for loop
    F = f.(X, t+tau)            # N
    U = cg(A, U + tau * F)      # N x (3+1)
\end{lstlisting}

\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}[fragile]
\frametitle{FDM: The Curse of Dimensionality}

\begin{lstlisting}[breaklines=true,frame=single,basicstyle=\footnotesize\ttfamily]
U = Vector{FType}(undef, N)     # N
buffer = similar(U)             # N
statevars = CGStateVariables(   # N x 3
    similar(U),
    similar(U),
    similar(U)
)
                        # total = N x 5
eval_on_grid!(n, d, U, u_IC, FType(0.0))
a_operator! = create_operator(n, d, r, FType)
A = LinearOperator( FType, N, N,
    true, true, a_operator!
)
for loop
    # eval U_t + tau * F_{t+1}
    eval_on_grid!(n, d, buffer, f, t+tau)
    buffer .*= tau
    buffer .+= U
    cg!(U, A, buffer; statevars=statevars);
\end{lstlisting}

\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{FDM: The Curse of Dimensionality}

\begin{figure}[H]
    \centering
    \includegraphics[width=80mm]{img/fdm_total_alloc.png}
    \label{fig:fdm_total_alloc}
    \caption{Total allocated memory for various discretizations. Equals the memory needed to allocate 5 vectors of size $N=n^d$.}
\end{figure}

\end{frame}
%%%%%%%%%%%%%%%% End

%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{FDM: The Curse of Dimensionality}

\begin{figure}[H]
    \centering
    \includegraphics[width=80mm]{img/fdm_cg_runtime.png}
    \label{fig:fdm_cg_runtim}
    \caption{Duration of a CG run (solving the system $A\,x=b$, where $A=I-\frac{\alpha \tau}{h^2}\,L,\:x=U^{t+1},\:b=U^t+\tau\,F^t$).}
\end{figure}

\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{Physics Informed Neural Networks (PINNs)}
Intro
- show diagram
- loss from NN
- how to train?
- sample points

Challanges in high dimensions
- laplace operator computation

How to handle them
- stoch laplace comp
- domain - based on loss
- parallel


Advantages / Disagvantages
\begin{itemize}
    \item flexibility
    \item memory efficient
\end{itemize}

Hardware requirements
- GPU(s)
\end{frame}
%%%%%%%%%%%%%%%% End


%%%%%%%%%%%%%%%% Begin
\begin{frame}
\frametitle{Next steps}

\begin{itemize}
    \item Fourier method - do not store the grid, only the coefficient of u in some basis.
    \item Tensor trains - write $v \in\mathbb{R}^{n^d}$ as a tensor $v \in \mathbb{R}^{n\times n\times ... \times n}$.
    \item Deep BSDE method - use the stochastic nature of the equation
\end{itemize}

\end{frame}
%%%%%%%%%%%%%%%% End





%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{document}
