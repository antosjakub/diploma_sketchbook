
\chapter{Heat Equation}

{
\color{red}
\begin{itemize}
\item class
\end{itemize}

}

\section{Formulation}
\begin{align*}
\partial_t u - \alpha \Delta u &= 0 \\
u(x) &= u_0(x) \\
u(x,t) &= 0
\end{align*}
where
\begin{align*}
\Omega &= [0,1]^d\\
T&=4.0 \\ 
\alpha &= \frac{\bar{\alpha}}{d}, \quad \bar\alpha>0 \\ 
u_0(x) &=
\prod_{i=1}^d \sin(\pi x_i)
+
\frac{0.67}{\sqrt{d}}
\sum_{i=1}^d \sin(2\pi x_i) \left(\prod_{k=1,k\neq i}^d \sin(\pi x_k)\right) \\
\end{align*}


The corresponding analytical solution in then (see the Appendix for detailed derivation)

$$
u(t,x) =
e^{-\gamma t}
\prod_{i=1}^d \sin(\pi x_i)
+
e^{-\delta t}
\frac{0.67}{\sqrt{d}}
\sum_{i=1}^d \sin(2\pi x_i) \left(\prod_{k=1,k\neq i}^d \sin(\pi x_k)\right)
$$
with
\begin{itemize}
\item $\gamma = \alpha d \pi^2 = \bar\alpha \pi^2$
\item $\delta = \alpha (d+3) \pi^2 = \bar\alpha (1+\frac{3}{d}) \pi^2$
\end{itemize}

Chosen so that it is slighlty more interesting then just a product of sines.


\section{Behavior in general dimension}

$$
u((0.5,\dots,0.5), t) = e^{-\gamma t} = e^{-\bar\alpha \pi^2}
$$

The decay rate, $\gamma$, of the heading term is independent of dimension.
If $\alpha$ did not depend on $d$, the solution would decay exponentially with dimension,
leading to negligile values even for relatively modest values of $d$.
Hence, we consider $\alpha=\frac{\bar\alpha}{d}$.

\textbf{L2 norm} - see Appendix
$$
||u(t,\cdot)||_{L^2(\Omega)}^2 =
\frac{1}{2^d}
\left(
e^{-\gamma t}
+
0.67^2
e^{-\delta t}
\right)
$$

\textbf{Loss}
The loss is computed as



\section{PINNs}

%\subsection{Curse of dim}
%
%two plots?

%\subsection{Experiments}

In all the experiments below, we consider:

\begin{itemize}
\item latin hypercube sampling (LHS)
\item learning rate (lr) of 1e-3
\item hard constrains for the boundary:
$$
u_\theta(t,x) = \hat{u}_\theta(t,x) \cdot \prod_{i=1}^d \sin(\pi x_i)
$$
\end{itemize}

\section{2-dimensional calibration case}

Guided by the base PINN architecture mentioned in <chaper of pinns>, we set the following hyperparametrs:
\begin{center}
\begin{tabular}{ |c|c|c|c } 
\hline
NN size & steps & number of collation points, or batch size (bs)\\
\hline
4x64  & 10,000 & 2048 \\
\hline
\end{tabular}
\end{center}
where by '4x64' mean a neutral network with 4 hidden layers, each consisting of 64 neurons.

Expomential lr decay, with a frequncy of 10 times during the training.

We also fix sample the dataset once, at the begging of training.

Also, the collations points for the initial conditon is 8 times less than that for pde.

For the comparison test, if not stated otherwise we perform 3 runs with separete seed and report the average of the runs.

The reported $L^\infty$ and relative $L^2$ erros are both computed form a collection of
100,000 points sampled using the LHS throughout the entire spatial-temporal domain.
If not stated otherwise, the $L^\infty$ is given as $\max(L^\infty_{pde}, L^\infty_{ic})$, and
the relative $L^2$ error as the average of the corresponding PDE and IC relative $L^2$ errors.

\subsection{Grid search}
In the machine learing community, many parameters to tune...
This is called a grid search.


\subsubsection{Loss weights: $\lambda_{pde}, \lambda_{ic}$}
As discussed in <chapter of pinns>, the loss landacape is perticularly sensitive
to the weight above the individual loss terms. in our case just $\lambda_{pde}, \lambda_{ic}$.
To determine an optimal, or at least close to optimal value of those weights, we first perform
a grid search over various differnt values. To this extend, we fix $\lambda_{pde}=1.0$, and carry out
the grid search over different values of $\lambda_{ic}$,
spanning 5 orders of magnitude.
%(with 3 random seeds).
As seen in \ref{fig:lambda_ic__GS}, the value $\lambda_{ic}=0.1$ gives the best results.
Hence we fix this value for the following runs.

\begin{figure}
    \centering
    \includegraphics[width=150mm]{figures/lambda_ic__GS.png}
    \caption{Plot of lambda 0.1 loss and linf run}
    \label{fig:lambda_ic__GS}
\end{figure}


\subsection{Loss landscape}
%bs: 1024 2048 4096

\begin{figure}
    \centering
    \includegraphics[width=150mm]{figures/bs_gs_training_loss.png}
    \includegraphics[width=150mm]{figures/bs_gs_test_rel_l2.png}
    \caption{Loss and bs, and rel l2}
    \label{fig:bs_gs}
\end{figure}

We observe that the loss is oscillaring widely messy \ref{fig:bs_gs}.
And, in our expeirmens, this is irrespective of the value of $\lambda_{ic}$
As we consider a fixed dataset, 2048 points spanning the entire spatial temporal domain,
the insufficeint number of collacation points point be the cause.
TO this effect, we compare our previous run with that of batch size 1024, and 4096.

To our surprise, 
the final errors and the oscillations do seem to be much correlated with the bs,
which is suprising given that we a small, fixed number of points from the domain.

What we notice, however, is that the smaller batchsizes run much faster on CPU,
which is not that suprising given the sequential nature of the hardware.
On GPU, the situation is very different. Since the model and the dataset are both quite small,
all the size of the batch size does not have significant effect on the runtime.

\begin{center}
\begin{tabular}{ |c|c|c|c|c| } 
\hline
batch size & time [cpu run] & time[gpu run] & rel $L^2$ & $L^\infty$ \\
\hline
1024 & 108s & ?s & 0.617e-3 & 1.27e-3 \\
\hline 
2048 & 168s & ?s & 0.684e-3 & 1.44e-3 \\
\hline 
4096 & 292s & ?s & 0.7e-3 & 1.3e-3 \\
\hline
\end{tabular}
\end{center}

As we perform some of our experiments on a cpu only,
to use the rousources most efficiently,
we select batch size of 1024 for the preceeding experiment.


%
%and the difference between the resampling and fixed datset regimes.
%Most efficient bs?
%
%average over 3 seeds again
%
%bs effect on training dynamics, hence apart from the error,
%we also report time.
%bs: 512 vs 2048 vs 8096
%
%We keep the the number of steps fixed (10k) - fixed n of optimizer steps.
%- bs = 512, ...
%
%%To this effect we consider two modes of grid search.
%%Second, fix the n of points that pass through the network during training.
%%- bs = 512 --> nsteps = 40k
%%- bs = 2048 --> nsteps = 10k
%%- bs = 8096 --> nsteps = 2.5k
%The batch size is the botthle neck?
%If we look at the memoty log, the GPU VRAM usage a peaked at 60MB. During the run...
%However, we see that smaller batch sizes lead to smaller $L^2$ and $L^\infty$ errors.
%The GPU needs much large batch size
%%\begin{figure}
%%    \centering
%%    \includegraphics[width=150mm]{figures/training_loss.png}
%%    \caption{loss minibatch, fixed}
%%    \label{fig:rar-d}
%%\end{figure}
%on gpu:
%even thought the number of collocation points is the same, 
%too small peak gpu mem utilization
%larger bs ?
%loss landscape ?
%does not matter that much, 1024 seem like a good compromise


Now, we seek to further adress the oscialltiong loss landscape.
We explore a diffeent strategy to managing the
oscialltions. (and note the effect on the $L^2$ and $L^\infty$ errors.)

In our base architecture, we exponentially decrease the learning rate every 1000 setps, meaning 10 times during the training.
We now change this rate to 20. We observe tha the learning rate does indeed have and effect on
the magnitude and frequency of oscillations, however, the general dynamics of the loss - the platou followed by a huge spike - stay the same.
We will return to this issue in the next section, here, we just seek to improve the error by
increasing the trainign horizoon to 20k steps.
The loss and erorr metrics platou, and hence they decline quite negligily.

The results are as follows:
\begin{center}
\begin{tabular}{ |c|c|c|c|c| } 
\hline
number of steps & $\ell_{total}$ & rel $L^2$ & $L^\infty$ \\
%10k & 1.815e-7 & 6.17e-4 & 1.27e-3 \\
10k & 3.258e-7 & 8.27e-4 & 1.8e-3 \\ % 25x
\hline
20k & 5.487e-8 & 4.12e-4 & 1.21e-3 \\ % 25x
\hline
\end{tabular}
\end{center}


\subsection{Stochasticity}
Now, we introduce stochasticity.
Instead of a fixed set of callocation points,
we resample the dataset every epoch.
\footnote{Actually, to avoid unnecessary overhead such frequent samplng would introduce into the training pipeline,
we sample every 100 steps a dataset of size $100\cdot$bs, and then give a batch of sampled point to the model every step}.

\begin{figure}
    \centering
    \includegraphics[width=150mm]{figures/training_losses_fixed_vs_resample.png}
    \caption{Loss dynamics: lr decay frequency and resampling}
    \label{fig:lambda_ic__GS}
\end{figure}

As expected, this dramatically changes the loss dynamics and decrease the infrequent high spikes in the loss,
allbeit for introducing more randomness into the training prodecure.
We observe that, again, the batch sizes do not have much effect on the the accuracy of the model, and the
leraning rate decay frequency influences the model equivalently as in the case of fixed dataset.

In accordance with the fixed dataset above, to impore the accuracy of the model, we next
increase the number of steps to 20,000 and further tune the learning rate decay to match this new paradigm run.

We report a good middle ground at 25 - which is approximatly 7\% of the starting learning rate.


The obtained loss and error metrics are given below:

\begin{center}
\begin{tabular}{ |c|c|c| } 
\hline
number of steps & $\ell_{total}$ & rel $L^2$ & $L^\infty$ \\
\hline
10k & 5.21e-7 & 1.10e-3 & 2.12-3 \\ %20x resample
\hline
20k & 1.42e-7 & 4.8e-4 & 1.1e-3 \\ % 30x resample
\hline
\end{tabular}
\end{center}
% gridsearch__26-07-08--10:11:39__GS_6_bs_and_resample

We have more control over the loss landacape her - RAR, causal, ...
Lets use it for now.

% /home/antos_j/diploma_sketchbook/PINN/case0_HeatEq/gridsearch__2026-07-09--11-27-08_resampl_decay750_20k/seed=42__prevent_resampling=False__bs=1024__n_res_points=102400/report.json


% if we try gradnorm, the lambda is optimal - goes to 10 vs 1 but same res


%\subsubsection{LBFGS}
%
%\paragraph{LBFGS alone}
%We first consider a pure LBFGS run.
%LBFGS expects large collation size, hence we slighlty increase the
%batches of sizes to 2k and 4k. And, since one lbfgs step can consist
%of multiple line search steps, we select a relatively modes 500 steps.
%
%Super quick convergence.
%line search 0 only a few steps ater the initia period.
%After that the line search only did not find any better minimum
%
%Suprisingly, we observed no noticable change between the two batch sizes.
%What we noticed, though was a significant worsening of the
%relative $L^2$ error for the initial conditon.
%Guide by this we increased the $\lambda_{ic}$ to the default 1.0, and the 
%performaced imporved by almost an ordr of magnitude - even below the pde one, without affecting the value of pde erros.
%
%\begin{center}
%\begin{tabular}{ |c|c|c|c|c|c| } 
%\hline
%$\ell_{pde}$ & $\ell_{ic}$ & $rel L^2$ pde & $rel L^2$ ic & $L^\infty$ pde & $L^\infty$ ic \\
%\hline
%2.8e-7 & 2.77e-7 & 0.000604 & 0.0009 & 0.00134 & 0.0014 \\
%\hline
%\end{tabular}
%\end{center}
%% /home/antos_j/diploma_sketchbook/PINN/case0_HeatEq/run_latest_l3_bs4000_dec50/report.json
%
%\paragraph{Adam + LBFGS}
%We run adam for until adam 1e-5 and 1e-6.
%At end of each run, we obtain the following errors:
%
%\begin{center}
%\begin{tabular}{ |c|c|c|c|c|c| } 
%\hline
%terminal loss value & steps & $rel L^2$ pde & $rel L^2$ ic & $L^\infty$ pde & $L^\infty$ ic \\
%\hline
%$1e-5$ & 2384 & 0.00811 & 0.00924 & 0.01693 & 0.01719 \\
%\hline
%$1e-6$ & 6121 & 0.00266 & 0.00379 & 0.00563 & 0.00567 \\
%\hline
%\end{tabular}
%\end{center}
%% /home/antos_j/diploma_sketchbook/PINN/case0_HeatEq/run_latest__adam1e-5/report.json
%[show plot: adam_e-5,6_starting_points - add lines to 1e-5,6]
%
%% consider $\lambda_{ic}=0.1, 1$ or only 1.0 ?
%
%For 
%We then fix the batch size of 2048, and 4096.
%Then run lbfgs for 250 (since 500 super flat) steps with a few random seeds,
%starting either from adam 1e-5 or 1e-6.
%
%All four resulting runs give almost equivalent results.
%Regardless of whethet we start from adam 1e5 or 6 and regardless of the batch size.
%
%Almost 10x decreate in all error metrics:
%
%The most efficient choice:
%2048, 1e-5, (lambda 1.0)
%\begin{center}
%\begin{tabular}{ |c|c|c|c| } 
%\hline
%$rel L^2$ pde & $rel L^2$ ic & $L^\infty$ pde & $L^\infty$ ic \\
%\hline
%0.000503 & 0.000490 & 0.000995 & 0.000843 \\
%\hline
%\end{tabular}
%\end{center}
%% /home/antos_j/diploma_sketchbook/PINN/case0_HeatEq/gridsearch__2026-07-09--13-08-44__lbfgs_GS_adam1e-5,6_42/lambda_ic=1p0__n_res_points=204800__bs=2048__starting_model=run_latest__adam1e-5-model.pth/report.json
%
%
%
%We naturally pick the combiantion of adam for till 1e-5, followed by LBFGS for 250 steps.
%
%[figure: pure adam, pure lbfgs, mix]
%
%
%bottom line:
%- minimizer of L2
%- prefers lambda 1.0



\subsection{4 Dimensions}
%Here, we reuse the same optimal net architecutre and hyperparameter choices
%decay 25
%n steps, NN, bs
%We run adam for 20k, 40k and nn 4x64 and 4x128
%testing point to 1M of the spatial temporal domain.
%In addition we try lbfgs, also for 4x64 and 4x128.
%Adam till 1e-5, followed by LBFGS for 250 steps.

%/storage/praha1/home/simulant_antos/diploma_sketchbook/PINN/case0_HeatEq/gridsearch__26-07-09--20:01:51__GS_d4/

Here, we reuse the optimal set of hyperparameters we arrived at in the previous case, namely
$\lambda_{ic}=0.1$, decay frequency 25.

We also test try to increase the network size, from 4x64 to 4x128,
effectively quadrupelling its capacity. To account for this large network,
we consider a case where we entend the training horizon from 20k to 40k.
The results are shown in \ref{fig:models_d4}. Of the 4 combinaitons,
it is apparent that the incresed training horizon leads to better results
irrespective of the model capacity, and leads to approximately 2x lower errors.
As for the model capacity, it was a surprise to see the smaller model
perform so well. In addition, we observe that loss correlates with the error quite well.
Below we report the two best runs, the two network sizes, each run for 40k steps.
We also report the used GPU memory (VRAM). The runtime - for both about 540s.
Compared to the 2-dimensional case, we report the error as so (averaged over the random seeds):
\begin{center}
\begin{tabular}{ |c|c|c|c|c| } 
\hline
network size & $\ell_{total}$ & rel $L^2$ & $L^\infty$ & allocated/reserved VRAM\\
\hline
4x128 & 3.88e-8 & 1.62e-3 & 2.55e-3 & 120/200 MB\\ 
\hline
4x64 & 1.126-7 & 1.89e-3 & 3.04e-3 & 71/124 MB\\ 
\hline
\end{tabular}
\end{center}

\begin{figure}
    \centering
    \includegraphics[width=150mm]{figures/model_comparison_for_d=4.png}
    \caption{Error and loss for various hyperparameter choices}
    \label{fig:models_d4}
\end{figure}




\subsection{6 Dimensions}

bs diff,
sample ic more if looking like it

increase network size, number of steps, and number of testing points again?

10M testing pnts

VRAM memory needs

% mistake in running..., very good - same 1024 bs, new data every 100 steps
% what bout periodically giving the nn new data?

%\subsection{8th dimension}
so far linear / exp. increase in training steps
exp increase in model size
scale gets us far - the equavalent of the scaling laws hold
but long training....

Q: Whether we can me the training more efficiet (smller model size, shorter training time)?
causal weighs & causal sampling
RAR
SDGD


\section{Sparse grid combiantion technique}

Crank nicholson
sparse matrix of the discreeized operaotr
Here, we consider GMRES with no preditioner. (ilu)
We tried CG with iLU but GMRES was much faster with same accuracy.

Set of time windows we sample there.
ic,

as we know the analytic sol, then 4 slices, where 1 at term time t=T

%we neding
%$r=\alpha \frac{\Delta t}{\Delta x^2}$
%$$
%\mu = \frac{a\Delta t}{(\Delta x)^2}
%$$
%actually not since crank nic stable irrespective of \Delta t

\subsection{2-dimensional calibraiton case}
[show plot of the solution at lvl=1,lvl=2,lvl=3 side by side - to see the basis functions / grid points at play]
[show plot linf and rel l2 err vs lvl -- goes down exponentially]

We set $\Delta t=0.01$, and benchmeark various levels $n$.
What we notice is the while the runtime increeses linearly $n$, the error
decreases with $n$ exponentially, see fig TD.

To our initial surprise, the largest was achieved at the initial condition,
and the errors decreased as $t\to T$.
This can be explain by the smoothing nature of the heat equaiton.

In addtion, setting smaller timestep $t=0.001$ had negligile effect on the solution accuracy.
(due to using the implicit scheme?)

\subsection{4 dimensions}
can run on 12 core AMD laptop without an issue
with 8 parallel workers and 12GB ram

\begin{center}
\begin{tabular}{ |c|c|c|c|c|c|c| } 
\hline
level $n$ & rel $L^2$ & $L^\infty$ & \# component grids & \# grid points & peak RAM [MB] & runtime \\
\hline
4 & 0.0241626 & 0.0506153 & 7 & 113 & 104.867 & 0.194734 \\
\hline
5 & 0.0072504 & 0.0155873 & 9 & 257 & 105.066 & 0.203949 \\
\hline
6 & 0.00209899 & 0.00464856 & 11 & 577 & 105.273 & 0.190572 \\
\hline
7 & 0.00060241 & 0.00131465 & 13 & 1281 & 105.902 & 0.220332 \\
\hline
8 & 0.000170055 & 0.000358433 & 15 & 2817 & 109.199 & 0.282883 \\
\hline
\end{tabular}
\end{center}

\begin{center}
\begin{tabular}{ |c|c|c|c|c|c|c| } 
\hline
level $n$ & rel $L^2$ & $L^\infty$ & \# component grids & \# grid points & peak RAM [MB] & runtime \\
\hline
4 & 0.0776054 & 0.152767 & 35 & 2769 & 113.605 & 1.30406 \\
\hline
5 & 0.0301881 & 0.0628298 & 69 & 7681 & 121.98 & 2.56963 \\
\hline
6 & 0.0110807 & 0.0238831 & 121 & 20481 & 150.711 & 5.96036 \\
\hline
7 & 0.00389236 & 0.00863072 & 195 & 52993 & 241.293 & 18.4484 \\
\hline
8 & 0.00132089 & 0.00297608 & 295 & 133889 & 527.609 & 71.1404 \\
\hline
\end{tabular}
\end{center}


need to set higher level to achieve the same accuracy?
in higher dimensions?


\subsection{6 dimensions}
THe few lelvels are managable on laptop, but as we saw in the previous case,
to achieve higher accuracy, we need to explore higher levels.
To this extend, we now result to running our experiments on cluster [cite metacentrum]

1M test points
set 24 workers?

consider levels 4:12
managable?
just set some fixed n of workers,

i am interested in:
    - accuracy at diff levels
    - n of grids / pnts





\text{Plot - max mem during run}
run till reach loss 1e-4

\chapter{Appendix}
% https://chatgpt.com/share/6a481b37-c04c-83eb-a0e8-b34c1d0ac08c

\section{Heat equation: Derivation of the $L^2$ norm of the analytic solution}
Consider the analytical solution
\begin{align*}
u(t,x)
&=
e^{-\gamma t}
\prod_{i=1}^d \sin(\pi x_i)
+
e^{-\delta t}
\frac{0.67}{\sqrt d}
\sum_{i=1}^d
\sin(2\pi x_i)
\left(
\prod_{\substack{k=1 \\ k\neq i}}^d \sin(\pi x_k)
\right)
\end{align*}
where
\[
\Omega = [0,1]^d
\qquad
\gamma = \alpha\pi^2
\qquad
\delta = \alpha\left(1+\frac 3d\right)\pi^2
\]
Here, we derive an explicit expression for the $L^2$ norm
\[
\|u(t,\cdot)\|_{L^2(\Omega)}^2
=
\int_{[0,1]^d} |u(t,x)|^2\,dx
\]
Define
\[
\phi_0(x)
=
\prod_{i=1}^d \sin(\pi x_i)
\]
and
\[
\phi_i(x)
=
\sin(2\pi x_i)
\prod_{\substack{k=1 \\ k\neq i}}^d \sin(\pi x_k)
\]
Then the solution can be written compactly as
\[
u(t,x)
=
e^{-\gamma t}\phi_0(x)
+
\frac{0.67}{\sqrt d}e^{-\delta t}
\sum_{i=1}^d \phi_i(x)
\]
Therefore
\begin{align*}
\|u(t,\cdot)\|_{L^2}^2
&=
\left\|
 e^{-\gamma t}\phi_0
 +
 \frac{0.67}{\sqrt d}e^{-\delta t}
 \sum_{i=1}^d \phi_i
\right\|_{L^2}^2
\end{align*}
Expanding the expression gives
\begin{align*}
\|u(t,\cdot)\|_{L^2}^2
&=
e^{-2\gamma t}\|\phi_0\|_{L^2}^2
+
2e^{-\gamma t}e^{-\delta t}\frac{0.67}{\sqrt d}
\sum_{i=1}^d \langle \phi_0,\phi_i\rangle_{L^2}
\notag \\
&\quad+
\frac{0.67^2}{d}e^{-2\delta t}
\sum_{i=1}^d\sum_{j=1}^d
\langle \phi_i,\phi_j\rangle_{L^2}
\end{align*}
We now use the orthogonality relation
\[
\int_0^1 \sin(m\pi x)\sin(n\pi x)\,dx
=
\begin{cases}
0, & m\neq n \\
\dfrac12, & m=n
\end{cases}
\]
Hence
\begin{align*}
\langle \phi_0,\phi_i\rangle_{L^2}&=0
\qquad \text{for all } i=1,\dots,d \\
\langle \phi_i,\phi_j\rangle_{L^2}&=0
\qquad \text{for } i\neq j
\end{align*}
The remaining terms give
\begin{equation*}
\|\phi_0\|_{L^2}^2
=
\int_{[0,1]^d}
\prod_{i=1}^d \sin^2(\pi x_i)\,dx
=
\prod_{i=1}^d
\int_0^1 \sin^2(\pi x_i)\,dx_i
=
\frac{1}{2^d}
\end{equation*}

\begin{equation*}
\|\phi_i\|_{L^2}^2
=
\left(
\int_0^1 \sin^2(2\pi x_i)\,dx_i
\right)
\prod_{\substack{k=1 \\ k\neq i}}^d
\left(
\int_0^1 \sin^2(\pi x_k)\,dx_k
\right)
=
\frac{1}{2^d}
\end{equation*}

\begin{equation*}
\sum_{i=1}^d\sum_{j=1}^d
\langle \phi_i,\phi_j\rangle_{L^2}
=
\sum_{i=1}^d \|\phi_i\|_{L^2}^2
=
d\frac{1}{2^d}
\end{equation*}
Substituting into the orignal expression, we obtain
\begin{align*}
\|u(t,\cdot)\|_{L^2}^2
&=
e^{-2\gamma t}\frac{1}{2^d}
+
\frac{0.67^2}{d}e^{-2\delta t}
\,
d\frac{1}{2^d}
\end{align*}
Thanks to the normalization $1/\sqrt d$, the factor $d$ cancels.
Therefore
\[
\|u(t,\cdot)\|_{L^2}^2
=
\frac{1}{2^d}
\left(
 e^{-2\gamma t}
 +
 0.67^2 e^{-2\delta t}
\right)
\]
At $t=0$, this simplifies to
\[
\|u_0\|_{L^2}^2
=
\frac{1}{2^d}
(1+0.67^2)
\]
